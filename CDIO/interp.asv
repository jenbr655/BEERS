function newImage = interp(im,idx)

[rows,cols] = size(im);
new_im = zeros(rows,cols);

idx = find(im > 0);
for i = length(idx)
[row_idx, col_idx] = ind2sub(size(idx),i);
im(idx(i)) = mean(mean(-10:idx(i)


